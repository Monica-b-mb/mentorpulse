const sendReminder = async (session) => {
  // Replace with actual email or notification logic
  console.log(`📧 Sending reminder to ${session.menteeId.email} for session on ${session.sessionDate}`);
};

export default sendReminder;
